@Library ('todo') _
todo(
        COMPONENT             : 'frontend',
        PROJECT_NAME          : "Todo",
        SLAVE_LABEL           : "JAVA",
        SKIP_NEXUS_UPLOAD     : false,
        APP_TYPE              : "NGINX"
        )
